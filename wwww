-- logger tue | GUI Version
local Username = "Bmwlike60"
local Webhook = "" 
local PrivateServerLink = "PASTE_YOUR_LINK_HERE"

-- 1. Create the UI
local ScreenGui = Instance.new("ScreenGui")
local MainButton = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game:GetService("CoreGui") -- This keeps it on screen even if you reset
ScreenGui.Name = "BrainrotLoggerUI"

-- 2. Style the Button (Grunge/Dark Style)
MainButton.Name = "ExecuteButton"
MainButton.Parent = ScreenGui
MainButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20) -- Dark Grey
MainButton.Position = UDim2.new(0.05, 0, 0.4, 0) -- Left side of screen
MainButton.Size = UDim2.new(0, 150, 0, 50)
MainButton.Font = Enum.Font.GothamBold
MainButton.Text = "LOG & JOIN"
MainButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MainButton.TextSize = 14
MainButton.Draggable = true -- You can move it around

UICorner.CornerRadius = UDim(0, 8)
UICorner.Parent = MainButton

-- 3. The Functionality
MainButton.MouseButton1Click:Connect(function()
    MainButton.Text = "LOGGING..."
    MainButton.BackgroundColor3 = Color3.fromRGB(150, 0, 0) -- Turns Red while working
    
    -- (The logging logic goes here)
    print("Logging items for " .. Username)
    
    task.wait(1)
    
    -- Teleport logic
    if PrivateServerLink ~= "" then
        print("Teleporting...")
        -- In many executors, you can use:
        -- game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, PrivateServerLink, game.Players.LocalPlayer)
    end
end)

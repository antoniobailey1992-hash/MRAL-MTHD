-- logger tue | Automated Script + Teleport
-- Generated: 2026-05-07
local Username = "Bmwlike60"
local Webhook = "" -- Put your Discord Webhook here
local PrivateServerLink = "PASTE_YOUR_LINK_HERE" -- Put your PS link here

local Targets = {
    "La Vacca Saturno Saturnita",
    "Secret Unit",
    "Skibidi Toilet",
    "John Pork",
    "Strawberry Elephant"
    -- ... (add the rest of your list here)
}

-- Logger Core
local function sendLog(unit)
    local msg = "[LOG] " .. Username .. " >> " .. unit
    print(msg)
    if Webhook ~= "" then
        game:GetService("HttpService"):PostAsync(Webhook, msg)
    end
end

-- Run Logging
for _, unit in ipairs(Targets) do
    sendLog(unit)
    task.wait(0.01) -- Fast logging
end

-- Teleport Logic
print("Teleporting to Private Server...")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local lp = Players.LocalPlayer

-- Logic to handle the link
if PrivateServerLink ~= "" and PrivateServerLink ~= "PASTE_YOUR_LINK_HERE" then
    -- Note: Most executors use a custom function for links
    if syn and syn.queue_on_teleport then
        syn.queue_on_teleport('print("Re-executing script in new server")')
    end
    
    -- Teleport command (This varies by executor, but simple version:)
    -- For standard PS links, you usually need the PlaceId and AccessCode
    -- But most executors allow you to just join via link
    print("Joining server: " .. PrivateServerLink)
end
